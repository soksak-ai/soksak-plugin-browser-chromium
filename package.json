{
  "name": "soksak-plugin-browser-cef",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "soksak browser plugin — in-process Chromium (CEF) child rendered as a content view",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "esbuild": "^0.24.2",
    "typescript": "~5.9.3"
  }
}
